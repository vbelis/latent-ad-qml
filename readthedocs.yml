version: 1

build:
  image: latest

sphinx:
    builder: html
    configuration: docs/source/conf.py
    fail_on_warning: true

python:
    install:
        - method: pip
          path: .
          extra_requirements:
              - docs
    system_packages: true